<!-- index.erb -->
<% t = Time.new %>
Hello, the time in London is currently <%= t.strftime("%l:%M %P on %A, %B %d, %Y") %><br><br>

The time in Seattle is currently <%= t.localtime.strftime("%l:%M %P on %A, %B %d, %Y") %>